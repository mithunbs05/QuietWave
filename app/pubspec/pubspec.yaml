name: quiet_wave_app
description: Quiet Wave Mobile App

publish_to: 'none'

environment:
  sdk: ">=2.17.0 <3.5.0"

dependencies:
  flutter:
    sdk: flutter
  mqtt_client: ^10.0.0
  provider: ^6.0.5
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
